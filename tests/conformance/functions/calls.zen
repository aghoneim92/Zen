fn factorial(n: Int) -> Int {
    if n <= 1 {
        return 1;
    }
    return n * factorial(n - 1);
}

fn sum(a: Int, b: Int) -> Int {
    return a + b;
}

fn one() -> Int {
    return 1;
}

fn main() -> Int {
    return sum(factorial(10), one());
}
